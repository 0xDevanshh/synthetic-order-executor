/**
 * Typed contract handles for SyntheticOrderExecutor, QuoterV2 and ERC20s,
 * built from the ABIs synced out of Hardhat by `pnpm abi:sync`.
 */

// TODO(impl): getContract({ address: EXECUTOR_CONTRACT_ADDRESS,
//             abi: syntheticOrderExecutorAbi, client }) plus helpers for
//             isOrderConsumed / balances / paused reads used by pre-flight and
//             reconciliation.
export {};
