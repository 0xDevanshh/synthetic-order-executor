/**
 * execute-order (concurrency 1) — NOT YET IMPLEMENTED.
 *
 * Will call ExecutionService.executeOrder (already implemented in the backend
 * package), which performs the atomic TRIGGERED -> EXECUTING claim, builds
 * calldata and submits to SyntheticOrderExecutor.
 */
export {};
