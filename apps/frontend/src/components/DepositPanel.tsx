/**
 * Deposit / withdraw against the executor vault.
 *
 * Worth stating plainly in the UI: funds sit in the user's own vault balance,
 * only the user can withdraw, and withdrawal works even while the contract is
 * paused.
 *
 * TODO(impl)
 */
export function DepositPanel() {
  return null;
}
