/**
 * Order detail: status timeline, every execution attempt, Etherscan links for
 * each tx hash, and the decoded revert reason on failure.
 *
 * Surfacing all attempts (not just the last) is deliberate — an execution
 * engine that hides its retries is impossible to trust or debug.
 *
 * TODO(impl)
 */
export default function OrderDetailPage() {
  return null;
}
