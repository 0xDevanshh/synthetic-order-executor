/**
 * Live ETH/USD from GET /prices/:pair.
 *
 * Shows tick age and the suspect flag — if the oracle is stale the user should
 * see why their order is not firing, rather than assuming the system is broken.
 *
 * TODO(impl)
 */
export function PriceTicker() {
  return null;
}
